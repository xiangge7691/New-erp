# Entity类与数据库字段同步计划

## 背景

数据库修复脚本`fix_database.sql`已执行成功，所有表结构已统一。现在需要修改Entity类以匹配新的数据库结构。

## 需要修改的Entity类（共36个文件）

### 1. 主键重命名（3个文件）

| 文件 | 旧主键 | 新主键 |
|------|--------|--------|
| `UserRole.java` | `userrole_id` / `userroleId` | `id` / `id` |
| `RolePerm.java` | `roleperm_id` / `rolepermId` | `id` / `id` |
| `UserDepartment.java` | `userdepartment_id` / `userdepartmentId` | `id` / `id` |

### 2. 新增 is_deleted 字段（36个文件）

以下Entity类需要添加 `is_deleted` 字段：

`ApprovalNode`, `ApprovalRecord`, `Department`, `DosageForm`, `Equipment`, `FileInfo`, `Material`, `Permission`, `PlanStatusLog`, `Preparation`, `PreparationFormula`, `ProcessType`, `ProductionPlan`, `ProductionProcessRecord`, `ProductionUnit`, `ProdUnitInvoice`, `ProdUnitMaterialFile`, `PurchaseOrderItems`, `PurchaseOrders`, `PurchaseSuppliers`, `Role`, `RolePerm`, `RoomInfo`, `Stock`, `StockIn`, `StockInDetail`, `StockOut`, `StockOutDetail`, `StockTransaction`, `Unit`, `User`, `UserDepartment`, `UserRole`, `WorkOrder`

添加字段：
```java
@TableField(value = "is_deleted")
private Integer isDeleted;
```

### 3. 新增 version 字段（42个文件）

以下Entity类需要添加 `version` 字段：

`ApprovalInstance`, `ApprovalNode`, `ApprovalRecord`, `ApprovalWorkflow`, `CleanInspectionRecord`, `Department`, `DisinfectionRecord`, `DosageForm`, `Equipment`, `FileInfo`, `Material`, `Permission`, `PlanStatusLog`, `Preparation`, `PreparationFormula`, `PressureDifferenceRecord`, `ProcessType`, `ProductionPlan`, `ProductionProcessRecord`, `ProductionUnit`, `ProdUnitInvoice`, `ProdUnitMaterialFile`, `PurchaseOrderItems`, `PurchaseOrders`, `PurchaseSuppliers`, `Role`, `RolePerm`, `RoomInfo`, `Stock`, `StockIn`, `StockInDetail`, `StockOut`, `StockOutDetail`, `StockTransaction`, `TemperatureHumidityRecord`, `Unit`, `User`, `UserDepartment`, `UserRole`, `WorkOrder`

添加字段：
```java
@TableField(value = "version")
private Integer version;
```

### 4. 新增特定业务字段

#### `Department.java` - 新增6个字段
```java
@TableField(value = "parent_id")
private Long parentId;

@TableField(value = "status")
private Integer status;

@TableField(value = "sort_order")
private Integer sortOrder;

@TableField(value = "created_at")
private LocalDateTime createdAt;

@TableField(value = "updated_at")
private LocalDateTime updatedAt;
```

#### `Unit.java` - 新增4个字段
```java
@TableField(value = "status")
private Integer status;

@TableField(value = "created_at")
private LocalDateTime createdAt;

@TableField(value = "updated_at")
private LocalDateTime updatedAt;
```

#### `DosageForm.java` - 新增4个字段
```java
@TableField(value = "status")
private Integer status;

@TableField(value = "created_at")
private LocalDateTime createdAt;

@TableField(value = "updated_at")
private LocalDateTime updatedAt;
```

#### `PurchaseOrders.java` - 新增9个字段
```java
@TableField(value = "supplier_id")
private Long supplierId;

@TableField(value = "prod_unit_id")
private Long prodUnitId;

@TableField(value = "approval_instance_id")
private Long approvalInstanceId;

@TableField(value = "created_by")
private Long createdBy;

@TableField(value = "updated_by")
private Long updatedBy;

@TableField(value = "created_at")
private LocalDateTime createdAt;

@TableField(value = "updated_at")
private LocalDateTime updatedAt;
```

#### `PurchaseOrderItems.java` - 新增2个字段
```java
@TableField(value = "material_id")
private Long materialId;

@TableField(value = "unit_id")
private Long unitId;
```

#### `PurchaseSuppliers.java` - 新增4个字段
```java
@TableField(value = "created_at")
private LocalDateTime createdAt;

@TableField(value = "updated_at")
private LocalDateTime updatedAt;
```

#### `StockIn.java` - 新增1个字段
```java
@TableField(value = "approval_instance_id")
private Long approvalInstanceId;
```

#### `StockOut.java` - 新增1个字段
```java
@TableField(value = "approval_instance_id")
private Long approvalInstanceId;
```

#### `ProdUnitMaterialFile.java` - 新增1个字段
```java
@TableField(value = "file_path")
private String filePath;
```

#### `EquipmentMaintenance.java` - 新增2个字段
```java
@TableField(value = "maintenance_company")
private String maintenanceCompany;

@TableField(value = "contact_phone")
private String contactPhone;
```

#### `RoomInfo.java` - 新增5个GMP字段
```java
@TableField(value = "production_type")
private String productionType;

@TableField(value = "dust_room")
private Boolean dustRoom;

@TableField(value = "clean_area")
private Boolean cleanArea;

@TableField(value = "clean_grade")
private String cleanGrade;

@TableField(value = "clean_procedure_file_id")
private Long cleanProcedureFileId;
```

#### `Equipment.java` - 新增4个维保字段
```java
@TableField(value = "equipment_type")
private String equipmentType;

@TableField(value = "maintenance_cycle")
private Integer maintenanceCycle;

@TableField(value = "reminder_days")
private Integer reminderDays;

@TableField(value = "next_maintenance_date")
private LocalDate nextMaintenanceDate;
```

## 执行顺序

1. 先修改3个主键重命名的文件（UserRole, RolePerm, UserDepartment）
2. 批量添加 is_deleted 字段
3. 批量添加 version 字段
4. 添加各表特定的业务字段

## 验证方式

1. 运行 `./mvnw compile` 确保编译通过
2. 运行 `./mvnw test` 确保测试通过
3. 启动应用确认无报错
