# 生产计划工序记录批量保存接口

## 需求

新增批量保存工序记录接口，前端保存生产计划时可一次性提交所有工序。

## 实施方案

### 1. `ProductionProcessRecordService.java` — 新增接口方法

```java
/**
 * 批量保存工序记录（先删后增）
 * @param planId 生产计划ID
 * @param records 工序记录列表
 * @return 保存的记录列表
 */
List<ProductionProcessRecord> batchSaveByPlanId(Integer planId, List<ProductionProcessRecord> records);
```

### 2. `ProductionProcessRecordServiceImpl.java` — 实现批量保存

```java
@Override
@Transactional(rollbackFor = Exception.class)
public List<ProductionProcessRecord> batchSaveByPlanId(Integer planId, List<ProductionProcessRecord> records) {
    // 1. 删除该计划下已有的工序记录
    QueryWrapper<ProductionProcessRecord> wrapper = new QueryWrapper<>();
    wrapper.eq("plan_id", planId);
    this.remove(wrapper);
    
    // 2. 设置公共字段
    Long currentUserId = EntityUtils.getCurrentUserId();
    LocalDateTime now = LocalDateTime.now();
    for (ProductionProcessRecord record : records) {
        record.setPlanId(planId);
        if (currentUserId != null) {
            record.setCreatedBy(currentUserId);
            record.setUpdatedBy(currentUserId);
        }
        record.setCreatedTime(now);
        record.setUpdatedTime(now);
    }
    
    // 3. 批量插入
    this.saveBatch(records);
    return records;
}
```

### 3. `ProductionProcessRecordController.java` — 新增批量保存端点

```java
/**
 * 批量保存工序记录（先删后增）
 * @param planId 生产计划ID
 * @param records 工序记录列表
 * @return 保存结果
 */
@PostMapping("/batch/plan/{planId}")
public ApiResponse<List<ProductionProcessRecord>> batchSaveByPlanId(
        @PathVariable Integer planId,
        @RequestBody List<ProductionProcessRecord> records) {
    try {
        List<ProductionProcessRecord> result = productionProcessRecordService.batchSaveByPlanId(planId, records);
        return success(result, "批量保存成功");
    } catch (Exception ex) {
        return exception(ex, "批量保存工序记录");
    }
}
```

### 4. 数据库迁移（如需要）

如果 `production_process_record` 表缺少 `process_type_id` 列，需执行：
```sql
ALTER TABLE `production_process_record` 
  ADD COLUMN `process_type_id` BIGINT DEFAULT NULL COMMENT '工序类型ID' AFTER `plan_id`;
```

## 调用示例

```
POST /api/process-record/batch/plan/123
Content-Type: application/json

[
  {
    "processTypeId": 1,
    "processName": "提取",
    "stepOrder": 1,
    "workshop": "提取一室",
    "processingQty": 100,
    "unitName": "kg"
  },
  {
    "processTypeId": 2,
    "processName": "浓缩",
    "stepOrder": 2,
    "workshop": "浓缩室",
    "processingQty": 50,
    "unitName": "L"
  }
]
```

## 验证

1. `./mvnw compile` 确认编译通过
2. 调用 `POST /api/process-record/batch/plan/{planId}` 测试批量保存
3. 调用 `GET /api/process-record/plan/{planId}` 验证返回的工序列表正确
