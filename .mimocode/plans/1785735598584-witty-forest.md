# 新增字段计划：工艺记录关键工艺参数 + 制剂销售单价

## 背景结论

1. **工艺记录（/api/process-record）**：`production_process_record` 表新增「关键工艺参数」字段 `process_params`（TEXT 长文本）——用户已确认。
2. **制剂（/api/preparation）**：`preparation` 表新增「销售单价」字段 `sales_price` DECIMAL(10,2)——用户已确认，命名与 `work_order.sales_price` 保持一致。
3. **采购计划（/api/purchase-plan）**：审批意见字段 `approvalOpinion` **已存在**（实体 `PurchasePlan.java:158`、数据库 `sql/purchase_module.sql`），且 `PUT /api/purchase-plan/{id}/status` 已支持传 `approvalOpinion`（Controller L110-120、ServiceImpl `updateStatus` L75-104）。**确认已实现，无需任何改动。**

## 改动方案（与既有 `equipment_params` 迁移同模式：Entity + SQL 脚本，MyBatis-Plus 自动处理 CRUD）

### 1. Entity 修改

**文件**：`src/main/java/com/tonghui/erp/Data/Entity/ProductionProcessRecord.java`

在 `equipmentParams` 字段（L92-93）之后新增：
```java
/**
 * 关键工艺参数
 */
@TableField(value = "process_params")
private String processParams;
```

**文件**：`src/main/java/com/tonghui/erp/Data/Entity/Preparation.java`

在 `retailPrice` 字段（L144-145）之后新增：
```java
/**
 * 销售单价
 */
@TableField(value = "sales_price")
private BigDecimal salesPrice;
```

> 无需改 Controller / Service / DTO：两模块均无独立 AddDto/UpdateDto，Controller 直接用实体，`PreparationWithDetailsDto` 继承 `Preparation` 自动带上新字段；MyBatis-Plus 自动生成 CRUD SQL 覆盖新列。

### 2. 数据库迁移 SQL（新增文件，遵循 sql/ 目录按表命名惯例）

**文件**：`sql/production_process_record_add_fields.sql`
```sql
-- 为 production_process_record 表添加关键工艺参数字段
-- 创建时间：2026-08-03

ALTER TABLE `production_process_record`
ADD COLUMN `process_params` TEXT NULL DEFAULT NULL COMMENT '关键工艺参数' AFTER `equipment_params`;
```

**文件**：`sql/preparation_add_fields.sql`
```sql
-- 为 preparation 表添加销售单价字段
-- 创建时间：2026-08-03

ALTER TABLE `preparation`
ADD COLUMN `sales_price` DECIMAL(10,2) NULL DEFAULT NULL COMMENT '销售单价' AFTER `retail_price`;
```

### 3. 不改动的文件（说明）

- `PurchasePlan.java` / `PurchasePlanServiceImpl.java` / `PurchasePlanController.java`：审批意见功能已存在，保持原样。

## 验证

1. `./mvnw compile` 编译通过（无类型/映射错误）。
2. 在生产/开发数据库手动执行两个迁移 SQL 脚本。
3. 接口验证（需先登录获取 JWT，请求头带 `Authorization: Bearer <token>`）：
   - `POST /api/process-record` 提交带 `processParams` 的请求体 → `GET /api/process-record/{id}` 能读回该字段。
   - `POST /api/preparation` 提交带 `salesPrice` 的请求体 → `GET /api/preparation/{id}` 能读回该字段。
   - `PUT /api/purchase-plan/{id}/status` 请求体 `{"status":"已审批","approvalOpinion":"同意"}` → `GET /api/purchase-plan/{id}` 返回 `approvalOpinion`（回归确认，不改动）。
