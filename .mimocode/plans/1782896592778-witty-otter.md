# Plan: 房间关联子表默认倒序

## 修改内容（1 处）

| 文件 | 行号 | 当前 | 改为 |
|------|------|------|------|
| `Service/impl/EquipmentServiceImpl.java` | 136 | `.orderByAsc(Equipment::getEquipmentName)` | `.orderByDesc(Equipment::getEquipmentId)` |

## 不需要修改（已是倒序）

- TemperatureHumidityRecord `findByRoomId` → `orderByDesc("record_date")`
- PressureDifferenceRecord `findByRoomId` → `orderByDesc("record_date")`
- CleanInspectionRecord `findByRoomId` → `orderByDesc("inspection_date")`
- DisinfectionRecord `findByRoomId` → `orderByDesc("disinfection_date")`
- CleaningRecord `findByRoomId` → `orderByDesc("cleaning_date")`
- EquipmentMaintenance `findByEquipmentId` → `orderByDesc("maintenance_date")`

## 不需要修改（提醒查询，正序合理）

- CleaningRecord `findUpcomingCleaning` → `orderByAsc("next_cleaning_date")` 即将到期在前
- DisinfectionRecord `findUpcomingDisinfection` → `orderByAsc("next_disinfection_date")` 即将到期在前
- EquipmentMaintenance `findUpcomingMaintenance` → `orderByAsc("next_maintenance_date")` 即将到期在前

## 验证
```bash
./mvnw compile
```
